{
  "name": "clickandcare",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "nodemon backend/server.js",
		"build": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin && npm run dev --prefix frontend  && npm run dev --prefix admin"
  },
  "author": "",
  "license": "ISC"
}
